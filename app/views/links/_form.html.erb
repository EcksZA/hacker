<%= form_for [:user, @link] do |f| %>
  <div class='form-group'>
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>
  <div class='form-group'>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <%= f.submit(:class => 'btn btn-success') %>
  </div>
<% end %>
